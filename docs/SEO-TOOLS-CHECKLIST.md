

---

## ریدایرکت

- [x] ساخت، ویرایش و حذف ریدایرکت ۳۰۱ و ۳۰۲ در `/admin/redirects`
- [x] ورود دسته‌ای به شکل `مبدأ مقصد کد`
- [x] اعمال ریدایرکت روی صفحات عمومی سایت
- [ ] ورود مستقیم فایل صادرات رنک‌مث
- [ ] ریدایرکت آمادهٔ مسیرهای مردهٔ وردپرس با وضعیت ۴۱۰ (`wp-login`، `wp-admin`، `xmlrpc`، `wp-json`، فید، پیوست، آرشیو نویسنده و تاریخ)
- [ ] ریدایرکت `/product/{slug}/`، دستهٔ محصول، صفحه‌بندی `/page/2/` و `/wp-sitemap.xml` از روی دادهٔ واقعی
- [ ] یکسان‌سازی اسلش انتهایی و ۳۰۱ سمت مخالف
- [ ] بازنویسی لینک‌های داخل متن و `srcset`، جدا از جدول ریدایرکت
- [ ] هشدار زنجیرهٔ ریدایرکت در پنل

## متا و ایندکس

- [x] عنوان، توضیح، کلمهٔ کلیدی و تصویر OG برای نوشته
- [x] عنوان، توضیح، تصویر OG و چهار حالت robots برای برگه
- [x] چاپ title، description، canonical، robots، Open Graph و توییتر در قالب سایت
- [x] پیش‌نمایش پیش‌نویس نوشته با `noindex`
- [x] `noindex` بودن خود پنل ادمین
- [ ] فیلد سئو در فرم دوره (ستون دیتابیس هست، فرم ادمین فیلد ندارد)
- [ ] robots جدا برای هر نوشته و هر دوره
- [ ] canonical اختصاصی هر URL، غیر از آدرس فعلی درخواست
- [ ] `noindex` برای جستجو، سبد، تسویه و پنل کاربر
- [ ] حذف `utm`، `ref`، `add-to-cart` و `replytocom` از canonical
- [ ] تصویر توییتر در قالب `rahbarhesab` (عنوان توییتر هست، `twitter:image` نیست)
- [ ] متای وریفای سرچ کنسول

## سایت‌مپ و robots

- [x] ایندکس سایت‌مپ و فایل جدا برای پست، برگه و دوره
- [x] `lastmod` هر پست، برگه و دوره از `updated_at`
- [ ] `lastmod` ایندکس سایت‌مپ از تاریخ واقعی، نه زمان حال
- [ ] حذف URLهای ریدایرکت‌شونده از نقشه (`/products`، `/services`، `/why-bisan` و محصولات بیسان)
- [ ] یک `robots.txt` روی دامنهٔ اصلی با خط `Sitemap: https://rahbarhesab.com/sitemap_index.xml` (فایل ثابت `public/robots.txt` هنوز لوکال‌هاست را معرفی می‌کند)
- [ ] `noindex` اجباری محیط تست، تا به پروداکشن نشت نکند

## دادهٔ ساخت‌یافته

- [x] `BreadcrumbList`
- [x] `Article` برای نوشته، با تاریخ انتشار
- [x] `Organization` و `WebSite` در صفحهٔ اصلی
- [x] `FAQPage` برای سؤال‌های صفحهٔ اصلی
- [x] `Course` برای صفحهٔ دوره
- [ ] واحد پول اسکیمای دوره هم‌خوان با تومان دیتابیس (الان عدد تومان با برچسب `IRR` می‌رود)
- [ ] `dateModified` در اسکیمای مقاله
- [ ] هم‌خوانی بردکرامب دیداری با `BreadcrumbList` بعد از ورود منو و محتوای واقعی
- [ ] نساختن `FAQPage` وقتی همان سؤال‌ها روی صفحه دیده نمی‌شوند

## محتوایی که امتیاز را همراه خودش می‌آورد

- [x] چکیده، تاریخ انتشار و انتشار زمان‌بندی‌شدهٔ نوشته
- [x] تصویر شاخص و متن جایگزین در فرم نوشته
- [x] کتابخانهٔ رسانه با فیلد alt
- [x] دستهٔ نوشته
- [x] مدیریت منو، شامل زیرمنو
- [ ] انتقال عنوان، توضیح، canonical، robots و OG از رنک‌مث
- [ ] انتقال نوشته، برگه و دوره با اسلاگ، تاریخ و متن وردپرس
- [ ] انتقال alt تصاویر از وردپرس
- [ ] حفظ یا ریدایرکت مسیر `uploads/سال/ماه`
- [ ] اسلاگ فارسی دقیقاً به شکل ایندکس‌شده
- [ ] جایگزینی فرم‌های المنتور که روی صفحات ایندکس‌شده هستند

## اندازه‌گیری

- [ ] نصب GA4 با کد خود تیم سئو
- [ ] نصب تگ منیجر، اگر روی وردپرس هست
- [ ] رویداد `view_item`، `add_to_cart`، `begin_checkout`، `purchase` بعد از پرداخت تأییدشده
- [ ] Clarity یا Hotjar، فقط اگر الان روی سایت زنده هستند

## سرعتِ مرتبط با سئو

- [x] `font-display: swap` برای فونت قالب
- [x] `loading="lazy"` روی کارت دوره و کارت بلاگ
- [ ] preload فایل فونت اصلی
- [ ] lazy نشدن تصویر بالای اولین نمای صفحه
- [ ] alt واقعی روی تصاویر لیست بلاگ (الان خالی است)

---

جمع: ۲۵ مورد آماده است، ۲۹ مورد را باید بسازیم یا از وردپرس وارد کنیم.
